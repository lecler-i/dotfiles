text/html; w3m -I %{charset} -T text/html; copiousoutput; print=qutebrowser %s > /dev/null; needsterminal;

application/pdf; /usr/bin/zathura %s; print=/usr/bin/acroread %s

image/*; ~/.config/mutt/muttimage.sh %s ; copiousoutput
